<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alden's Dinner Routine</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Lexend', sans-serif;
            background-color: #f0f9ff;
            touch-action: manipulation;
        }
        .task-card {
            transition: all 0.3s ease;
        }
        .task-card.completed {
            background-color: #d1fae5;
            border-color: #10b981;
            opacity: 0.7;
        }
        .confetti {
            position: fixed;
            width: 10px;
            height: 10px;
            background-color: #f00;
            z-index: 1000;
        }
        @keyframes fall {
            to { transform: translateY(100vh) rotate(360deg); }
        }
    </style>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-md mx-auto bg-white rounded-3xl shadow-xl overflow-hidden border-4 border-blue-200">
        <!-- Header -->
        <div class="bg-blue-500 p-6 text-white text-center">
            <h1 class="text-3xl font-bold mb-2">Alden's Routine 🌟</h1>
            <div class="w-full bg-blue-300 rounded-full h-4 mt-4 overflow-hidden">
                <div id="progress-bar" class="bg-yellow-400 h-full transition-all duration-500" style="width: 0%"></div>
            </div>
            <p id="stats" class="mt-2 text-sm font-medium">0 of 26 tasks done</p>
        </div>

        <!-- Routine List -->
        <div id="checklist" class="p-4 space-y-3 max-h-[70vh] overflow-y-auto">
            <!-- Tasks will be injected here -->
        </div>

        <!-- Footer -->
        <div class="p-4 bg-gray-50 border-t flex justify-between items-center">
            <button onclick="resetRoutine()" class="text-gray-500 text-sm font-bold uppercase tracking-wider">Reset All</button>
            <div id="completion-message" class="hidden text-green-600 font-bold italic">Sweet dreams Alden! 🌙</div>
        </div>
    </div>

    <script>
        const tasks = [
            { id: 1, text: "5:30 Milk, Juice, Dinner & Show", icon: "🍱" },
            { id: 2, text: "Turn off show", icon: "📺" },
            { id: 3, text: "Use the bathroom", icon: "🚽" },
            { id: 4, text: "Playtime and swing time", icon: "🪑" },
            { id: 5, text: "End swing time (6:30)", icon: "⏰" },
            { id: 6, text: "Goodnight downstairs", icon: "👋" },
            { id: 7, text: "Bring Count & Creeper Nikola upstairs", icon: "🧸" },
            { id: 8, text: "Go to the bathroom", icon: "🚽" },
            { id: 9, text: "Socks off", icon: "🧦" },
            { id: 10, text: "Pants off", icon: "👖" },
            { id: 11, text: "Shirt off", icon: "👕" },
            { id: 12, text: "Use the toilet", icon: "🚽" },
            { id: 13, text: "Wait until tub is full", icon: "🌊" },
            { id: 14, text: "Get in tub", icon: "🛀" },
            { id: 15, text: "Play", icon: "🦆" },
            { id: 16, text: "Wash", icon: "🧼" },
            { id: 17, text: "Out of tub into towel", icon: "🧖‍♂️" },
            { id: 18, text: "Go to bedroom", icon: "🛌" },
            { id: 19, text: "Drink milk", icon: "🥛" },
            { id: 20, text: "Pull up on", icon: "✨" },
            { id: 21, text: "Bottoms on", icon: "🩳" },
            { id: 22, text: "Top on", icon: "👕" },
            { id: 23, text: "Socks on", icon: "🧦" },
            { id: 24, text: "One last potty", icon: "🚽" },
            { id: 25, text: "Brush teeth", icon: "🪥" },
            { id: 26, text: "Turn on candle & off lamp", icon: "🕯️" },
            { id: 27, text: "Get in bed", icon: "🛌" },
            { id: 28, text: "Read books", icon: "📚" },
            { id: 29, text: "Blow out candle", icon: "🌬️" },
            { id: 30, text: "Goodnight song", icon: "🎵" },
            { id: 31, text: "Countdown to kisses", icon: "😘" }
        ];

        let completedTasks = new Set();

        function renderChecklist() {
            const container = document.getElementById('checklist');
            container.innerHTML = '';
            
            tasks.forEach(task => {
                const isDone = completedTasks.has(task.id);
                const div = document.createElement('div');
                div.className = `task-card flex items-center p-4 border-2 rounded-2xl cursor-pointer ${isDone ? 'completed border-green-500 bg-green-50' : 'border-blue-100 bg-white shadow-sm'}`;
                div.onclick = () => toggleTask(task.id);
                
                div.innerHTML = `
                    <div class="text-3xl mr-4">${task.icon}</div>
                    <div class="flex-1 font-bold text-lg ${isDone ? 'line-through text-green-700' : 'text-blue-900'}">
                        ${task.text}
                    </div>
                    <div class="w-8 h-8 rounded-full border-2 flex items-center justify-center ${isDone ? 'bg-green-500 border-green-500' : 'border-blue-200'}">
                        ${isDone ? '✓' : ''}
                    </div>
                `;
                container.appendChild(div);
            });
            updateProgress();
        }

        function toggleTask(id) {
            if (completedTasks.has(id)) {
                completedTasks.delete(id);
            } else {
                completedTasks.add(id);
                createConfetti();
            }
            renderChecklist();
        }

        function updateProgress() {
            const percent = (completedTasks.size / tasks.length) * 100;
            document.getElementById('progress-bar').style.width = `${percent}%`;
            document.getElementById('stats').innerText = `${completedTasks.size} of ${tasks.length} tasks done`;
            
            if (completedTasks.size === tasks.length) {
                document.getElementById('completion-message').classList.remove('hidden');
            } else {
                document.getElementById('completion-message').classList.add('hidden');
            }
        }

        function resetRoutine() {
            if(confirm("Start a new day?")) {
                completedTasks.clear();
                renderChecklist();
            }
        }

        function createConfetti() {
            for (let i = 0; i < 15; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.top = '-10px';
                confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 70%, 50%)`;
                confetti.style.borderRadius = '50%';
                confetti.style.animation = `fall ${Math.random() * 2 + 1}s linear forwards`;
                document.body.appendChild(confetti);
                setTimeout(() => confetti.remove(), 3000);
            }
        }

        // Initialize
        renderChecklist();
    </script>
</body>
</html>
